define host {

	use			root-checador		;
	host_name		Checadores		; The name of this host template
	alias			Checadores		;
	hostgroups		checadores		;
	max_check_attempts              1		;
	contact_groups                  admins                  ; Notifications get sent to the admins by default
}

