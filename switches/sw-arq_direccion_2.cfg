define host{
        use             generic-switch  		; Inherit default values from a template
        host_name       sw-arq_direccion_2	      	; The name we're giving to this switch
        alias           sw ARQUITECTURA DIRECCION 2	; A longer name associated with the switch
        address         148.216.45.7    		; IP address of the switch
        hostgroups      switches        		; Host groups this switch is associated with
        parents         sw-arq_direccion_1   	;
}

