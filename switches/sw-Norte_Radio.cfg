define host{
        use             generic-switch  		; Inherit default values from a template
        host_name       sw-Norte_Radio      		; The name we're giving to this switch
        alias           sw NORTE RADIO NICOLAITA    	; A longer name associated with the switch
        address         148.216.121.25			; IP address of the switch
        hostgroups      switches     		   	; Host groups this switch is associated with
        parents         black-diamond   		;
}

