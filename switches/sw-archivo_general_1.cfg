define host{
        use             generic-switch  				; Inherit default values from a template
        host_name       sw-archivo_general_1      			; The name we're giving to this switch
        alias           sw AECHIVO GENERAL 1			     	; A longer name associated with the switch
        address         148.216.84.1   				 	; IP address of the switch
        hostgroups      switches        				; Host groups this switch is associated with
        parents         sw-archivo_general_2   				;
}

