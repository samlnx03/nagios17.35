define host{
        use             generic-switch  				; Inherit default values from a template
        host_name       sw-archivo_general_2      			; The name we're giving to this switch
        alias           sw ARCHIVO GENERAL 2			     	; A longer name associated with the switch
        address         148.216.84.2   				 	; IP address of the switch
        hostgroups      switches        				; Host groups this switch is associated with
        parents         sw-Div_Pos_1   				;
}

