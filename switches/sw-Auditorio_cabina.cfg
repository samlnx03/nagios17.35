define host{
        use             generic-switch  				; Inherit default values from a template
        host_name       sw-Auditorio_cabina      			; The name we're giving to this switch
        alias           sw AUDITORIO CABINA			     	; A longer name associated with the switch
        address         148.216.83.1   				 	; IP address of the switch
        hostgroups      switches        				; Host groups this switch is associated with
        parents         sw-Pos_FIE_Nuevo   				;
}

